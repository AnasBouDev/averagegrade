import { defineConfig } from 'vite'

export default defineConfig({
    // Use relative base path so the site works no matter where it's hosted
    // (e.g. https://username.github.io/repo-name/)
    base: './'
})
